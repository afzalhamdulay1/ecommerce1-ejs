    this is the a file in which i have saved the deleted code for future use
    
    <!-- <nav class="w-full fixed top-0 left-0 px-5 py-3 z-10 bg-white">
        <div class="flex justify-between">
            <h3 class="text-xl">Baggers</h3>
            <% if (typeof(user) != "undefined" && user.fullname) { %>
                <h3>Welcome, <%= user.fullname %>!</h3>
            <% } else { %>
                <h3>Welcome, Guest!</h3>
            <% } %>
            <div class="flex gap-5">
                <% let isLoggedIn = typeof loggedIn !== "undefined" ? loggedIn : true %>
                <% if(isLoggedIn) { %>
                    <a href="/shop">Shop</a>
                    <a href="/cart">Cart</a>
                    <a href="">My Account</a>
                    <a href="/users/logout" class="text-red-600 bg-yellow-300 px-3 rounded-md">logout</a>
                <% } %>
                
            </div>
        </div>
        <div class="bg-yellow-400 text-center p-3 mt-2"><h2>This project is under development! So, some features might be missing.</h2></div>
        
    </nav> -->

    <!-- <nav class="w-full fixed top-0 left-0 px-5 py-3 z-10 bg-white">
    <div class="flex justify-between">
        <h3 class="text-xl">Baggers</h3>
        <div>
            <% if (user && user.fullname) { %>
                <h3>Welcome, <%= user.fullname %>!</h3>
            <% } else { %>
                <h3>Welcome, Guest!</h3>
            <% } %>
        </div>
        <div class="flex gap-5">
            <% if (user) { %>
                <a href="/shop">Shop</a>
                <a href="/cart">Cart</a>
                <a href="">My Account</a>
                <a href="/users/logout" class="text-red-600 bg-yellow-300 px-3 rounded-md">Logout</a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/register">Register</a>
            <% } %>
        </div>
    </div>
</nav> -->